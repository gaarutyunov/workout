name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages-deploy
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run build
        env:
          # GitHub Pages serves the project site under /<repo>/.
          VITE_BASE: /${{ github.event.repository.name }}/
          # The Dropbox app key is public (it ships in the bundle), so a repo
          # Variable is the natural home — but accept a Secret too, since that's an
          # easy mix-up. `vars.X || secrets.X` takes whichever is set.
          VITE_DROPBOX_APP_KEY: ${{ vars.VITE_DROPBOX_APP_KEY || secrets.VITE_DROPBOX_APP_KEY }}

      # SPA fallback so a hard refresh on a deep link still loads the app.
      - run: cp dist/index.html dist/404.html

      - name: Deploy to gh-pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist
          # Don't wipe PR previews on a main deploy.
          clean-exclude: pr-preview/
